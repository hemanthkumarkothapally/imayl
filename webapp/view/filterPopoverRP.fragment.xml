<!-- <core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Popover
        id="filterPopover"
        title="Selection Criteria"
        placement="Bottom"
        initialFocus="apply"
    >
        <footer>
            <OverflowToolbar>
                <Button
                    text="Cancel"
                    press=".handleCancelPress"
                />
                <ToolbarSpacer />
                <Button
                    id="apply"
                    text="Apply"
                    press=".handleApplyPress"
					type="Emphasized"
                />
            </OverflowToolbar>
        </footer>
		<HBox justifyContent="SpaceAround" width="20rem">
		<VBox >
        <Text text="From Date" />
        <DatePicker
            id="popoverfromdate"
            value="{path: 'requestpackageModel>/popoverfromdate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'MM/dd/yyyy' }}"
            valueFormat="MM/dd/YYYY"
            displayFormat="MM/dd/YYYY"
			width="8rem"
        />
		</VBox>
		<VBox >
        <Text text="To Date" />
		<DatePicker
            id="popovertoDate"
            value="{path: 'requestpackageModel>/popovertoDate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'MM/dd/yyyy' }}"
            valueFormat="MM/dd/YYYY"
            displayFormat="MM/dd/YYYY"
			width="8rem"
        />
		</VBox>
		</HBox>
		<VBox justifyContent="SpaceBetween" width="20rem">
		<Text text="Date Exception"></Text>
		<ComboBox selectedKey="All">
		<core:Item key="All" text="All"></core:Item>
		<core:Item key="RecipientEmailMissing" text="RecipientEmailMissing"></core:Item>
		<core:Item key="RecipientNameMissing" text="RecipientNameMissing"></core:Item>
		</ComboBox>
		</VBox>
		<VBox >
		<Text text="Ship To Name"></Text>
        <Input type="Text"></Input>
		</VBox>
    </Popover>
</core:FragmentDefinition> -->

<core:FragmentDefinition
    xmlns="sap.m"
    xmlns:core="sap.ui.core"
>
    <Popover
        id="filterPopover"
        title="Selection Criteria"
        placement="Bottom"
        initialFocus="apply"
        class="sapUiContentPadding" 
    >
        <!-- Footer -->
        <footer >
            <OverflowToolbar>
                <Button
                    text="Cancel"
                    press=".handleCancelPress"
                />
                <ToolbarSpacer />
                <Button
                    id="apply"
                    text="Apply"
                    press=".handleApplyPress"
                    type="Emphasized"
                />
            </OverflowToolbar>
        </footer>

        <!-- Content -->
        <VBox
            
            class="sapUiSmallMargin"
        >
            <!-- Date Range Section -->
            <HBox
                justifyContent="Start"
                alignItems="Center"
                class="sapUiSmallMarginBottom" 
            >
                <VBox alignItems="Start">
                    <Text text="From Date" />
                    <DatePicker
                        id="popoverfromdate"
                        value="{path: 'requestpackageModel>/popoverfromdate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'MM/dd/yyyy' }}"
                        valueFormat="MM/dd/YYYY"
                        displayFormat="MM/dd/YYYY"
                        width="8rem"
                    />
                </VBox>
                <VBox alignItems="Start">
                    <Text text="To Date" />
                    <DatePicker
                        id="popovertoDate"
                        value="{path: 'requestpackageModel>/popovertoDate', type: 'sap.ui.model.type.Date', formatOptions: { pattern: 'MM/dd/yyyy' }}"
                        valueFormat="MM/dd/YYYY"
                        displayFormat="MM/dd/YYYY"
                        width="8rem"
                    />
                </VBox>
            </HBox>

            <!-- Date Exception Section -->
            <VBox
                class="sapUiSmallMarginBottom" 
            >
                <Text text="Date Exception" />
                <ComboBox
                    selectedKey="All"
                    width="15rem" 
                >
                    <core:Item key="All" text="All" />
                    <core:Item key="RecipientEmailMissing" text="Recipient Email Missing" />
                    <core:Item key="RecipientNameMissing" text="Recipient Name Missing" />
                </ComboBox>
            </VBox>

            <!-- Ship To Name Section -->
            <VBox>
                <Text text="Ship To Name" />
                <Input
                    type="Text"
                    width="15rem" 
                />
            </VBox>
        </VBox>
    </Popover>
</core:FragmentDefinition>